from setuptools import setup, find_packages

setup(
    name="chaitorch",
    version="0.1",
    description="pytroch wrapper dll framework likely chainer",
    url="https://github.com/onnga-wasabi/chaitorch",
    packages=find_packages(),
)
